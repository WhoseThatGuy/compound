{
  "name": "compound-gym",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for Compound Gym, Dunedin. No dependencies by design.",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js && git diff --exit-code --stat"
  },
  "engines": {
    "node": ">=18"
  }
}
